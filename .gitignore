# dependencies
node_modules/
.venv/

# secrets
.env
session_cookies.json

# temp media (transcription audio, OCR frames, slide images) + ingest snapshots (tmp/ingest/)
tmp/

# debug logs (clean terminal = rich progress; full detail goes here)
logs/

# private collection data — generate with: python scripts/list_collections.py --update
config/collections.json

# private v2 config (commit the *.example.json templates only)
config/run.json
config/tags.json
config/routes.json

# python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# claude session notes — local scratch only
.claude/work/

# os
.DS_Store
